{
  "name": "skyshift-weather-check-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for hourly weather checks",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cd dist && powershell -Command \"Compress-Archive -Path * -DestinationPath ..\\function.zip -Force\" && cd .."
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "resend": "^6.4.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}

